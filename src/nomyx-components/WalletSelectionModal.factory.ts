export function createWalletSelectionModal(React: any) {
    // Import the component directly (it has its own React import)
    const { WalletSelectionModal } = require('./WalletSelectionModal');
    
    // Return the component directly
    return WalletSelectionModal;
}